# أرشيف البيانات — اقرأ قبل أي استخدام

## ⛔ هذه البيانات **قديمة** ولا تُستخدم

مجلد `deprecated/` يحتوي قواعد بيانات **مهجورة**. السبب موثَّق من صاحب المشروع:

> «القديمة التي فيها أكثر من ٥٠٠ خدمة فهي قديمة وتم التعديل عليها»

لذلك **لا تدمج محتواها في قاعدة العمل**، ولا تستخدمها مرجعاً للبيانات الحقيقية.

---

## ✅ القاعدة المعتمدة

| | |
|---|---|
| **الملف** | `winfeen/api/storage/app.sqlite` |
| **المحافظات** | حلب · **ديرالزور** |
| **المناطق** | ١٥٨ |
| **الخدمات** | ٣٩٢ |
| **الحقول الديناميكية** | ٥ حقول · ٣٤ خياراً |
| **اسم الموقع** | دليل الدير |

هذه هي النسخة الوحيدة المعتمدة، وهي المضمَّنة في `winfeen-upload.zip`.

---

## 🗂️ محتوى `deprecated/`

| الملف | ما هو |
|---|---|
| `app.deployed-halab-20260923.sqlite` | نسخة كانت منشورة على `t3lam.site/dalel` — ٥٢٣ خدمة، محافظتا حلب وريف حلب، حقل ديناميكي واحد |
| `app.before-fields-20260923-030524.sqlite` | نسخة ما قبل ترحيل الحقول (مأخوذة تلقائياً بواسطة `migrate_fields.php`) |
| `app.before-fields-20260923-031913.sqlite` | نسخة ما قبل ترحيل الحقول (مأخوذة تلقائياً بواسطة `migrate_fields.php`) |

### لماذا بقيت هنا؟

للرجوع إليها فقط — إن احتجت يوماً البحث عن بيانات قديمة لم تُنقل.
**لا تُرفع إلى الاستضافة، ولا تُدمج.**

---

## 🔍 الفرق بين النسختين

| | ⛔ المهجورة (deprecated) | ✅ المعتمدة |
|---|---|---|
| الخدمات | ٥٢٣ | **٣٩٢** |
| المحافظات | حلب · ريف حلب | حلب · **ديرالزور** |
| المناطق | ٩٠ | **١٥٨** |
| الحقول الديناميكية | ١ حقل (الاختصاص) | **٥ حقول · ٣٤ خياراً** |
| الكازيات | ٩٠ خدمة | ٠ |
| السرافيس | ٢٣ خدمة | ٠ |
| المستخدمون | ٧ | ٢ |

> ⚠️ **لا تستنتج أن المهجورة «أكمل»** — عدد خدماتها أكبر، لكنها بيانات قديمة جرى تعديلها، والعدد الأكبر لا يعني أنها الأصح.

---

## 📌 حالة القاعدة المعتمدة — تفصيل

```
المحافظات
  # 1  حلب        ← ٧٢ منطقة (مدن) · ٣٩٢ خدمة
  #10  ديرالزور   ← ٨٦ منطقة (٢٥ مدينة + ٥٩ ريفية + ٢ قرية) · ٠ خدمة
```

| القسم | الخدمات |
|---|---|
| صيدليات بشرية | ٢٤٢ (كلها في حلب) |
| عيادات أطباء | ١٥٠ (كلها في حلب) |
| كازيات | ٠ |
| سرفيس واسعافية | ٠ |
| المخابر | ٠ |

**المعنى:** هيكل ديرالزور (المحافظة + ٨٦ منطقة) جاهز لكن بلا خدمات بعد.
والأقسام الثلاثة الأخيرة فارغة في كل المحافظات.

`settings.tagline` ما زال يقول «…في مدينة حلب…» و`settings.city` فارغ — يُضبطان من لوحة الإدارة.

---

## 🛠️ إن أردت لاحقاً نقل بيانات محدَّدة من المهجورة

الأداة القديمة (`restore_halab_services.py`) **حُذفت** لأنها كانت ستنقل بيانات مهجورة كاملة — وهذا ما نبّهتَ ضده.

إن احتجت يوماً نقلاً انتقائياً (خدمة بعينها مثلاً) فأخبرني، وسأبني أداة نقل **محدودة ومراجعة يدوياً** بدل النقل الجماعي.
